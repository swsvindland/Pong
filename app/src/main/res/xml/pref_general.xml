<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="Medium"
        android:entries="@array/list"
        android:entryValues="@array/list"
        android:key="difficulty"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/ai_difficulty" />

    <ListPreference
        android:defaultValue="10"
        android:entries="@array/scores"
        android:entryValues="@array/scores"
        android:key="@string/score_to_win"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/score" />

</PreferenceScreen>
